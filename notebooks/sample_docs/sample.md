# Sample document

This is a placeholder document so the exploration notebook has something to
ingest out of the box. Replace `notebooks/sample_docs/` with your own
markdown files, or point `load_documents` at a different directory.
